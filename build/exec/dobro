#!/bin/sh
# @generated by Idris 0.8.0-214eb4547, Chez backend

set -e # exit on any error

if [ "$(uname)" = Darwin ]; then
  DIR=$(zsh -c 'printf %s "$0:A:h"' "$0")
else
  DIR=$(dirname "$(readlink -f -- "$0")")
fi
export LD_LIBRARY_PATH="$DIR/dobro_app:$LD_LIBRARY_PATH"
export DYLD_LIBRARY_PATH="$DIR/dobro_app:$DYLD_LIBRARY_PATH"
export IDRIS2_INC_SRC="$DIR/dobro_app"

"$DIR/dobro_app/dobro.so" "$@"